# 国际化内容维护指南

本项目使用**表格驱动**的国际化方案，内容制作者可以通过编辑 CSV 表格文件来管理多语言翻译内容。

## 📁 文件结构

```
src/i18n/content/           # CSV 表格内容目录
├── _common.csv             # 全局通用（按钮、loading 等）
├── _nav.csv                # 导航栏
├── _footer.csv             # 页脚、语言切换、主题切换
├── home.csv                # 首页 /
├── about.csv               # 关于页 /about
├── work.csv                # 作品页 /work
├── gallery.csv             # 画廊首页 /gallery
├── gallery-modules.csv     # 画廊模块页 /gallery/:module
├── gallery-submodules.csv  # 画廊子模块
├── case-index.csv          # 案例首页 /work/the-case
├── case-phase-01.csv       # 案例阶段1 /work/the-case/phase-01
├── case-phase-02.csv       # 案例阶段2
├── case-phase-03.csv       # 案例阶段3
└── case-phase-04.csv       # 案例阶段4

src/locales/                # 自动生成的 JSON 翻译文件（不要手动编辑）
├── zh/translation.json
└── en/translation.json
```

## 🔄 工作流程

```
1. 编辑 CSV 表格
       ↓
2. 运行命令: npm run i18n:build
       ↓
3. 自动生成 translation.json
       ↓
4. 刷新页面查看效果
```

## 📝 CSV 表格格式

每个 CSV 文件包含三列：

| key | zh | en |
|-----|----|----|
| 翻译键名 | 中文内容 | 英文内容 |

### 示例

```csv
key,zh,en
about.pageTitle,关于,About
about.greeting,你好，我是田阳敏,Hi, I'm Tian Yangmin.
```

### 处理特殊字符

如果内容包含逗号，需要用双引号包裹：

```csv
key,zh,en
home.heroDesc,"聚焦品牌设计、平面设计与 CMF，","Focused on brand design, graphic design, and CMF,"
```

### 数组语法

使用 `[索引]` 来表示数组元素：

```csv
key,zh,en
home.heroTags[0],品牌设计,Brand Design
home.heroTags[1],平面设计,Graphic Design
home.heroTags[2],CMF,CMF
```

### 嵌套对象

使用点分隔符表示嵌套：

```csv
key,zh,en
about.expertise.strategy.title,策略,Strategy
about.expertise.strategy.items[0],产品策略,Product Strategy
```

## 🛠 命令说明

### 构建翻译文件

```bash
npm run i18n:build
```

读取所有 CSV 表格，生成 `translation.json` 文件。

### 监听模式（开发中）

```bash
npm run i18n:watch
```

监听 CSV 文件变化，自动重新构建。

## 📂 表格与页面对应关系

| CSV 文件 | 对应页面 URL | 说明 |
|----------|-------------|------|
| `_common.csv` | 全局 | Loading、按钮、通用文案 |
| `_nav.csv` | 全局 | 导航栏链接 |
| `_footer.csv` | 全局 | 页脚、语言切换 |
| `home.csv` | `/` | 首页 |
| `about.csv` | `/about` | 关于页 |
| `work.csv` | `/work` | 作品页 |
| `gallery.csv` | `/gallery` | 画廊首页 |
| `gallery-modules.csv` | `/gallery/:module` | 画廊模块详情 |
| `gallery-submodules.csv` | `/gallery/:module/:sub` | 画廊子模块 |
| `case-index.csv` | `/work/the-case` | 案例研究首页 |
| `case-phase-01.csv` | `/work/the-case/phase-01` | 案例阶段1 |
| `case-phase-02.csv` | `/work/the-case/phase-02` | 案例阶段2 |
| `case-phase-03.csv` | `/work/the-case/phase-03` | 案例阶段3 |
| `case-phase-04.csv` | `/work/the-case/phase-04` | 案例阶段4 |

## ✏️ 编辑建议

### 使用 Excel 或 Google Sheets

1. 用 Excel/Google Sheets 打开 CSV 文件
2. 编辑中文和英文列
3. 保存为 CSV 格式（UTF-8 编码）
4. 运行 `npm run i18n:build`

### 使用 VS Code

推荐安装插件：
- **Rainbow CSV** - 高亮显示 CSV 列
- **Edit csv** - 表格形式编辑 CSV

## ⚠️ 注意事项

1. **不要手动编辑** `src/locales/` 下的 JSON 文件，它们会被覆盖
2. **保持 key 不变** - 只修改 zh/en 列的内容，不要修改 key 列
3. **UTF-8 编码** - 保存 CSV 时确保使用 UTF-8 编码
4. **备份文件** - 原始翻译文件已备份在 `backup/locales/` 目录

## 🔧 常见问题

### Q: 修改后页面没有更新？

1. 确认已运行 `npm run i18n:build`
2. 检查终端是否有错误信息
3. 清除浏览器缓存后刷新

### Q: CSV 格式错误？

检查以下几点：
- 包含逗号的内容是否用双引号包裹
- 文件编码是否为 UTF-8
- 是否有多余的空行

### Q: 如何添加新的翻译键？

1. 在对应的 CSV 文件中新增一行
2. 填写 key、zh、en 三列
3. 运行 `npm run i18n:build`
4. 在代码中使用 `t('新的.key')` 调用

## 📊 统计信息

当前共有 **14 个 CSV 文件**，包含约 **482 条翻译**。
